/**
 * 
 * @param shape {Object} - Object containing the shape of collection data.
 * @returns shape {Object} - Object representing the shape of collection data.
 */
export const shape = function(shape: object): object{
      return shape;
}

export const types = {
       string:  'string',
       number: 'number',
       boolean: 'boolean',
       array: 'array',
}